# awesome-dsh-plugin submission metadata
# ------------------------------------------------------------------
# After creating the public GitHub repo, submit a PR to
# https://github.com/awesome-dsh-plugin/awesome-dsh-plugin that adds
# ONE file:
#   data/plugins/ChaoJie0__dsh-tm-guard.yml
#
# The YAML below is ready to paste as-is into
#   data/plugins/ChaoJie0__dsh-tm-guard.yml
# (the owner field below already matches the repo URL).
#
# Submission requirements already met by this repo:
#   - package.json declares dsh.bundle.patch (installable via `dsh plugin add`)
#   - a cordis.patch.yml ships next to package.json
#   - real, working code (7 tm_* tools + pre-execute gate + audit)
# Requirements to check at PR time:
#   - repo is at least 1 day old (CI checks this automatically)
#   - repo has the GitHub topic: dsh-plugin
#   - package.json repository.url points back at this exact repo
# ------------------------------------------------------------------

url: https://github.com/ChaoJie0/dsh-tm-guard
name: ChaoJie0/dsh-tm-guard
category: security
description:
  en: 'Zero-intervention permission gate for DSH agents on macOS: auto-allows local writes made reversible by git or Time Machine, blocks network, package installs, process control and sensitive-path reads, with full audit logs.'
  zh: 'macOS 上 DSH 智能体的零干预权限门：可经本地 git 或时间机器回滚的本地写操作自动放行，拦截网络、装包、进程控制与敏感路径读取，并记录完整审计日志。'
