# 给 Agent 装上"安全带"：一套不用人盯着也不会闯祸的自治系统

> 📌 这份文档讲的是你电脑里 `dsh-tm-guard` 这个小玩意——它让 AI Agent（AI 助手）能在你 Mac 上**自己埋头干活、全程不用你点"允许"**，但又绝不会帮你把电脑搞坏、把密码偷走、把东西传到网上去。
>
> 全部内容都来自你这个文件夹里的真实代码和运行记录，不瞎编。

---

## 🎯 一句话说清楚

> **能"后悔"的事，放手让它干；不能"后悔"的事，一律拦下来。**

什么叫"能后悔"？就是 macOS 自带的 **Time Machine（时间机器）** 能把文件还原回去的操作——比如改个代码、存个文档、删个东西。

什么叫"不能后悔"？就是一旦做了就收不回来的事——比如上网发请求、装软件、杀进程、改系统设置、派小弟去干活。

```
            🤖 AI Agent 想干一件事
                      │
                      ▼
              ┌───────────────┐
              │  门口的"安检员" │  ← 就是这个插件
              └───────────────┘
                      │
          ┌───────────┴───────────┐
          ▼                       ▼
   📁 只是改本地文件?        🌐 要上网/装包/杀进程?
          │                       │
          ▼                       ▼
      ✅ 放行，先存档          🚫 当场拦下，记一笔
      （后悔药已备好）         （这事不能让你干）
```

---

## 🤔 为什么需要它？

直接把 AI Agent 放出去干活，就像把刚学会开车的人扔上早高峰环路：

| 直接放手的后果 | 有了这个系统之后 |
|---|---|
| 🤯 每点一下都弹"是否允许？"，你成了工具人 | 🚀 一次设置，它自己跑一下午 |
| 💥 改崩了代码，只能哭着重装 | 💾 改坏了一秒还原到上一步 |
| 🕵️ 它偷偷读了你的 `~/.ssh` 密钥 | 🔒 敏感目录连看都不让看 |
| 🌍 它自作主张 `npm install`、`git push` | 🚫 联网动作在门口就被掐了 |
| 🫣 干完活你都不知道它干了啥 | 📹 每一步都有监控录像可回放 |

---

## 🚦 安检员到底怎么判断放行？

它把 AI 的每个动作先"翻译"成几类，再对照这张表：

| 动作类型 | 例子 | 安检员态度 |
|---|---|---|
| 👀 只是看看 | `cat`、`ls`、`grep`、`git log` | ✅ 随便看 |
| ✏️ 改受保护目录里的文件 | 写文件、`rm`、`mv`、`sed -i` | ✅ 放行，**先存个档** |
| 📦 本地 git 操作 | `add`、`commit`、`reset` | ✅ 放行（纯文件操作，能后悔） |
| 🌐 联网 | `curl`、`wget`、`git push`、`ssh` | 🚫 不行 |
| 🧩 装软件 | `npm install`、`brew`、`pip` | 🚫 不行 |
| ⚙️ 动系统/进程 | `kill`、`launchctl`、`sudo`、`shutdown` | 🚫 不行 |
| 👶 派子代理 | `spawn`、`interrupt_agent` | 🚫 不行（小弟干了啥你查不到） |
| ❓ 看不懂的命令 | 任何生僻命令 | 🚫 **保守起见，一律先拒绝** |
| ☠️ 毁灭性操作 | `rm -rf /`、`mkfs` | 🚫 永远不行 |
| 🔑 偷看敏感目录 | `~/.ssh`、`~/.aws`、钥匙串 | 🚫 拒绝 + 记黑账 |

**一个小细节**：命令像 `cat 文件1 && curl 某网址 && echo done` 这种用 `&&` 串起来的，安检员会**一段一段拆开看，只要有一段有问题，整条命令全部拒绝**——别想靠"夹带"蒙混过关。

---

## 🔍 门口的"开箱验货"：脚本里偷偷打电话，也看得见

命令表有个天然盲区：安检员只看命令行上写了什么，可 `python3 干活.py` 这行字本身是干净的——真正的猫腻藏在 `干活.py` 里头。

```
   命令行：python3 干活.py        ← 表面上：跑个本地脚本，放行 ✅
   ─────────────────────────────
   干活.py 里：import requests
              requests.get("https://偷偷传你东西.com")   ← 以前看不见 👀
```

现在加了一道**开箱验货**：只要安检员打算放行"跑本地脚本"这类动作，它会顺手把脚本拆开看一眼——

```
   🤖 python3 干活.py
          │
          ▼
   ┌──────────────────────┐
   │  开箱验货（只读不跑）   │
   │  ① 脚本文件本身         │
   │  ② python -c "..." 内联 │
   │  ③ python - <<EOF 管道   │
   └──────────────────────┘
          │
   ┌──────┴───────┐
   │              │
 干净 ✅         发现网络调用 🚫
 放行干活        当场拦下，
                记一笔"脚本里有requests.get"
```

它认这些小动作：

| 脚本里干了啥 | 安检员 |
|---|---|
| `import requests` / `socket` / `aiohttp`，**而且真的发了请求到外网** | 🚫 拦下 |
| `subprocess.run(["curl", ...])` / `os.system("wget ...") | 🚫 拦下 |
| `eval(...)` / `exec(...)` / `__import__("requests")`——看不懂它到底要干嘛 | 🚫 保守拦下 |
| 只连 `127.0.0.1` / `localhost`（本地开发探针） | ✅ 放行，不冤枉 |
| 只读本地文件、算算数 | ✅ 放行 |

两条公道话：
- **只 import 不发请求**不报警——很多工具模块 `import requests` 只是留着备用；必须文件里真出现对外请求，才把那行 import 一起记账。
- **它会自己往下挖**：入口脚本 `import utils.py`，安检员会顺着把 `utils.py` 也读一遍（最深 3 层、最多 20 个文件，自动跳过 `node_modules`/`.venv`/`vendor`）；`npm run build` 会翻开 `package.json` 里那个脚本接着查；`pytest` 会把 `test_*.py` 收集起来一起看。

**它仍然不是沙箱**：靠正则看代码文本，运行时才拼出来的网址、`eval(解密后字节)`、`cargo run`/Makefile 里没解析到的目标，它看不见。macOS 自带的 `sandbox-exec` 实测在本机要么全封要么全放、不能只放 loopback，而 dsh 自己要连大模型，套不住整个进程。所以真正的系统调用层那道墙交给 **LuLu**（见下一节）。

---

## 🧱 最后一道墙：LuLu 网络出口（系统调用层兜底）

静态扫描再强，也是"读代码文本猜它要干嘛"——运行时才拼出来的地址，它看不见。真正的铁壁在网络出口：你已经装了 LuLu，按下面配好，Agent 的脚本就算把 `requests.get` 藏到天上去，出站那一刻也被内核掐掉。

```
   🤖 脚本里偷偷 requests.get("https://偷东西.com")
            │
            ▼
   🚪 分类器 + 静态扫描（读代码，提前拦/记账）
            │  万一漏过去
            ▼
   🧱 LuLu（看进程，系统调用层）── 非白名单域名直接断
```

**它认识谁、该放行谁**（你这台机器上的实测路径）：

- 要出网的只有 dsh 自己：`/opt/homebrew/bin/node`，它只连大模型 `ark.cn-beijing.volces.com`；
- Agent 跑的 `node` / `python3` / `bash` 按设计根本不需要出网——一律拒。

**在 LuLu 里这么配**（Preferences → Rules，从上往下匹配，Allow 排在 Deny 上面）：

| 顺序 | 进程 | 动作 | 目标 |
|---|---|---|---|
| 1 | `/opt/homebrew/bin/node` | ✅ Allow | `ark.cn-beijing.volces.com` |
| 2 | `/opt/homebrew/bin/node` | 🚫 Deny | `*`（除此域名，哪儿都别去） |
| 3 | `/usr/bin/python3`、`/usr/bin/python` | 🚫 Deny | `*` |
| 4 | `/bin/bash`、`/bin/zsh` | 🚫 Deny | `*` |

偏好里：默认出站动作设 **Block（每次问）**、勾上 **Allow loopback / local network**（本地开发服务器要用）。

**验一下墙牢不牢**：
```bash
# 应该被 LuLu 拦下/弹窗
/opt/homebrew/bin/node -e "fetch('https://example.com').then(r=>console.log(r.status))"
# 本地回环仍然通
curl -s http://127.0.0.1:端口/...
```

**两个坑**：
- LuLu 按**二进制路径**认进程，不看命令行参数——dsh 自己和 Agent 起的子 node 都是同一个 `/opt/homebrew/bin/node`，所以必须靠"Allow 方舟域名 + Deny `*`"这一对把它锁死，别图省事给 node 整个 Allow；
- 以后换了模型供应商，dsh 连不上时 LuLu 会弹窗，把新 host 加进第 1 条；Agent 脚本引发的弹窗一律点 Deny。

---

## 💾 三层"后悔药"——它根本不让你有机会后悔

这是整个系统最妙的地方：**你不用记着备份，它在三层自动帮你兜底。**

```
        ┌─────────────────────────────────┐
        │  第 3 层  🍎 macOS 时间机器      │  ← 系统自己每小时存一次
        │           （跟插件无关的老底）    │
        ├─────────────────────────────────┤
        │  第 2 层  ⚡ 写文件前自动存档    │  ← 关键！AI 想绕都绕不过
        │           （30 秒内不重复存）    │
        ├─────────────────────────────────┤
        │  第 1 层  📝 AI 主动存档        │  ← 系统提示它"动手前先存档"
        │           （比如叫"改登录模块前"）│
        └─────────────────────────────────┘
```

- **第 2 层是命根子**：不管 AI 听没听"动手前先存档"的话，只要它想写文件，门口的安检员**强制先按一下"存档键"**。AI 再怎么想野也野不出存档点。
- 30 秒内连着改很多次文件，只存一次（不然硬盘存爆了）。想每次改都存档？把 `snapshotCooldownSeconds` 设成 `0` 就行。

---

## 🛑 安全刹车：后悔药没了，就不许再动

安检员每次写文件前，都会先体检一下"后悔药还好不好使"：

```
        🚗 想踩油门（写文件）
                │
                ▼
        ┌──────────────┐
        │ 后悔药体检 🩺 │
        └──────────────┘
           │        │
         健康 ✅   不健康 🚫
           │        │
           ▼        ▼
        放行      🛑 紧急刹车
              （写操作全部拒绝，
                但还能读、能查状态）
```

体检三项：
- Time Machine 到底配没配备份盘？
- 本地有没有至少一个存档点？
- 你的工作目录有没有被 Time Machine 排除掉？

只要有一项不通过——**宁愿停工也不动手**。这就叫"故障安全（fail-closed）"：没有后悔药，就不开工。

---

## 🔁 AI 干活的固定五步曲

系统会告诉 AI：干活必须按这个剧本走，别乱来：

```
  ①存档          ②动手           ③验收           ④搞砸了          ⑤成了
 ┌──────┐   ┌──────┐   ┌──────┐   ┌──────┐   ┌──────┐
 │先拍张 │ → │改文件 │ → │跑测试 │ → │一键   │ → │再存个 │
 │"现场" │   │      │   │看结果 │   │还原！ │   │"完美态"│
 └──────┘   └──────┘   └──────┘   └──────┘   └──────┘
   tm_snapshot   自动放行   自己跑     tm_rollback  再存一次
```

铁律：
- 被拦下的动作**不许重试**，换个纯本地方案；
- 不许写工作区以外的文件；
- 没跑测试验证之前，**不准说"干完了"**。

---

## 🎚️ 真搞砸了，怎么还原？——两条路分工明确

Time Machine 有个短板：**它管不好"全新建出来的文件"**。所以系统配了两个还原手，各管一摊：

```
   🧤 git（秒还原、不用密码、离线）        ⏰ Time Machine（还原历史版本）
   ┌─────────────────────────┐          ┌─────────────────────────┐
   │ 撤销改动：git reset --hard │          │ 从已完成的备份里找旧版本  │
   │ 删掉新文件：git clean -fd │          │ （需要授权全盘访问）     │
   │ ✅ 管"改过的 + 新建的文件" │          │ ✅ 管"被改过的旧文件"    │
   └─────────────────────────┘          └─────────────────────────┘
```

> 还有第三条路（直接挂载快照）——但需要 root 密码，而这个系统偏偏把 `sudo` 封了，所以这条路**按设计就是走不通的**，不用纠结。

---

## 📹 它给自己装的"行车记录仪"

你最关心的问题：**"这一下午它到底干了啥？"** 系统留了三层记录：

```
   📝 每条动作都记账                    📊 每个回合自动出报告
   ┌─────────────────────┐            ┌─────────────────────┐
   │ audit.jsonl          │  ──────→  │ turn-xxx.md          │
   │ 谁、什么时候、干了啥、 │            │ 放行X次/拦了Y次/      │
   │ 存没存档、为啥被拦     │            │ 改了哪些文件          │
   └─────────────────────┘            └─────────────────────┘
                    │                            │
                    └──────────┬─────────────────┘
                               ▼
                    ✅ 独立验收（第三只眼）
                    ┌─────────────────────┐
                    │ 它说做了？我亲自去磁盘上核对：│
                    │ · 说产出的文件真在吗？非空吗？│
                    │ · 写文件有没有越界？        │
                    │ · 被拦的动作没留烂摊子？     │
                    │ · 后悔药（git+快照）就位吗？ │
                    └─────────────────────┘
```

也就是说——**AI 自己说"我干完了"不算数**，系统会拿文件系统的真实状态去对账，对不上就打个"⚠️ 有疑点"。

---

## 🧰 给 AI 发的 5 个遥控器

AI 自己会用这 5 个工具来管整个过程：

| 遥控器 | 干啥用 |
|---|---|
| 📸 `tm_snapshot` | 拍存档，还能贴个便签，比如"改登录模块前" |
| 📋 `tm_list_snapshots` | 看看都有哪些存档点 |
| ↩️ `tm_rollback` | 还原；说 `last_operation=true` 就撤销最近一次改动 |
| 🔍 `tm_audit` | 翻账本：这次到底放行/拦了哪些动作 |
| 🩺 `tm_status` | 一键体检：后悔药还好使吗？现在啥策略？ |

---

## 🧩 身边的小伙伴插件

原则很简单：**断网就是断网，只装"不联网也能干活"的纯本地小工具。**

```
  🛡️ 安全搭子（8个功能插件）
  ┌──────────┬──────────┬──────────┬──────────┐
  │ safeguard│  case    │ cron     │  fmt     │
  │ 危险拦截  │ 大小写转换│ 定时解析  │ 格式化   │
  ├──────────┼──────────┼──────────┼──────────┤
  │ handoff  │  jwt     │ password │  when    │
  │ 交接导出  │ 解码     │ 生成密码  │ 时间换算  │
  └──────────┴──────────┴──────────┴──────────┘

  🎨 体验搭子（界面好看用，web 版）
     侧边栏 / 全屏终端 / Git图谱 / 皮肤中心 / 桌宠
     使用热力图 / 上下文占用 / 长期记忆 …

  🚫 故意不装的：所有联网类插件（装了也被门口拦下，白费劲）
```

---

## 🩹 踩过的坑（时间线）

```
  2026-09-06  🔧 把"放行名单"配好，8 个插件全部跑通
              📝 敏感目录拦截实测：试了 6 种偷摸读密钥的姿势，全被拦下
              ✅ 分类器测试 59/59 全过；git 回滚、TM 回滚实测通过

  2026-09-06  ⚠️ 7 个插件一起崩——原因是新版把 CallId 改名叫 ToolCallId
              → 用 pnpm patch 永久修好，升级也不丢

  2026-09-07  💥 最惨一次：另一个插件挂了"拦截点"事件却没返回结果，
              整个 AI 直接崩溃，坏会话怎么救都救不回来
              → 血泪教训：挂拦截点的插件必须返回合法决策对象，
                不然就是给自己（和整个系统）埋雷
```

---

## 🚧 它管不了/不打算管的事

| 事儿 | 为什么 |
|---|---|
| ⏰ Time Machine 不是实时的 | 两小时之间的新改动，只能靠"写前自动存档"保护 |
| 💽 外接盘/网络盘 | 那些不在 Time Machine 保护范围内 |
| 🌍 想联网干活 | 这是设计上故意掐断的，要联网得自己关掉开关（不推荐） |
| 🤯 生僻命令 | 认不出来就先拦着——保守点总比闯祸强 |
| 🖥️ 整个系统盘还原 | 得进 macOS 恢复模式 |
| 👶 子代理干活 | 它干了啥门口看不见，所以默认不让派 |

---

## 📁 这套东西长什么样

```
   自治/
   ├── src/               ← 真正的大脑（8 个模块）
   │   ├── index.ts        门口安检员 + 5 个遥控器
   │   ├── classifier.ts   动作分类表
   │   ├── scan-scripts.ts 开箱验货：脚本内静态扫网络调用
   │   ├── tm.ts          时间机器工具包
   │   ├── audit.ts       行车记录仪
   │   ├── git.ts         git 自动存档
   │   ├── report.ts      每回合自动出报告
   │   └── verify.ts      第三只眼验收
   ├── cordis.patch.yml   ← 开机配置（保护哪些目录、拦哪些目录）
   ├── README…md          ← 更技术向的原始文档
   ├── test-*.mts         ← 各种自测脚本
   └── .tm-guard/
       ├── audit.jsonl     行车记录仪录像（已经十几 MB 了）
       └── reports/       每一回合的报告
```

---

## 🏁 总结成一张图

```
        🤖 AI 想干活
            │
            ▼
   🚪 安检员：能后悔吗？──不能──→ 🚫 拦下，记账
            │
          能后悔
            │
            ▼
   🩺 后悔药还好吗？──不好──→ 🛑 停工
            │
          好
            │
            ▼
   💾 自动存档 → ✏️ 放行干活 → 🧪 逼它自己验收
                                    │
                        ┌───────────┴───────────┐
                        ▼                     ▼
                     没通过                  通过
                        │                     │
                        ▼                     ▼
                   ↩️ git/TM 还原          📊 出报告 + ✅ 第三只眼核对
```

> **说白了**：这套系统就是给 AI 画了个圈——圈里随便造，圈外一步都别迈；每一步都留了"后悔药"和"监控录像"，真出事了一键还原。你该干嘛干嘛，不用盯着它。
